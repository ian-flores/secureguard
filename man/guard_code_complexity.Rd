% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code-complexity.R
\name{guard_code_complexity}
\alias{guard_code_complexity}
\title{Code complexity guardrail}
\usage{
guard_code_complexity(
  max_ast_depth = 50L,
  max_calls = 200L,
  max_assignments = 100L,
  max_expressions = 50L
)
}
\arguments{
\item{max_ast_depth}{Integer(1). Maximum allowed AST nesting depth.
Default \code{50}.}

\item{max_calls}{Integer(1). Maximum number of function calls allowed.
Default \code{200}.}

\item{max_assignments}{Integer(1). Maximum number of assignment operations.
Default \code{100}.}

\item{max_expressions}{Integer(1). Maximum number of top-level expressions.
Default \code{50}.}
}
\value{
A guardrail object of class \code{"secureguard"} with type \code{"code"}.
}
\description{
Creates a guardrail that checks R code against complexity limits using
AST statistics. Prevents overly complex or deeply nested code from being
executed.
}
\examples{
g <- guard_code_complexity()
run_guardrail(g, "x <- 1 + 2")
}
