% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{secure_pipeline}
\alias{secure_pipeline}
\title{Create a complete guardrail pipeline}
\usage{
secure_pipeline(
  input_guardrails = list(),
  code_guardrails = list(),
  output_guardrails = list()
)
}
\arguments{
\item{input_guardrails}{List of guardrails with \code{type = "input"}.}

\item{code_guardrails}{List of guardrails with \code{type = "code"}.}

\item{output_guardrails}{List of guardrails with \code{type = "output"}.}
}
\value{
A list with methods:
\itemize{
\item \verb{$check_input(text)}: run input guardrails, returns \code{check_all()} result.
\item \verb{$check_code(code)}: run code guardrails, returns \code{check_all()} result.
\item \verb{$check_output(result)}: run output guardrails via \code{guard_output()}.
\item \verb{$as_pre_execute_hook()}: convert code guardrails to a securer hook.
}
}
\description{
Bundles input, code, and output guardrails into a single pipeline object
with convenience methods for each stage.
}
\examples{
pipeline <- secure_pipeline(
  input_guardrails = list(guard_prompt_injection()),
  code_guardrails = list(guard_code_analysis()),
  output_guardrails = list(guard_output_pii())
)
pipeline$check_input("Hello, world")
pipeline$check_code("x <- 1")
}
