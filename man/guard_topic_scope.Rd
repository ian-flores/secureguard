% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-topic-scope.R
\name{guard_topic_scope}
\alias{guard_topic_scope}
\title{Topic scope guardrail}
\usage{
guard_topic_scope(
  allowed_topics = NULL,
  denied_topics = NULL,
  case_sensitive = FALSE
)
}
\arguments{
\item{allowed_topics}{Character vector of regex patterns. If non-\code{NULL},
input must match at least one pattern to pass. Cannot be used together
with \code{denied_topics}.}

\item{denied_topics}{Character vector of regex patterns. Input must not
match any of these patterns. Cannot be used together with
\code{allowed_topics}.}

\item{case_sensitive}{Logical(1). Whether pattern matching is
case-sensitive. Default: \code{FALSE}.}
}
\value{
A guardrail object of class \code{"secureguard"} with type \code{"input"}.
}
\description{
Creates a guardrail that restricts input to specific topics using allowlist
or denylist regex patterns.
}
\examples{
g <- guard_topic_scope(allowed_topics = c("statistics", "data analysis"))
run_guardrail(g, "How do I calculate a t-test in statistics?")
run_guardrail(g, "What is the weather today?")
}
