% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patterns-pii.R
\name{detect_pii}
\alias{detect_pii}
\title{Detect PII in text}
\usage{
detect_pii(text, types = NULL)
}
\arguments{
\item{text}{Character(1). The text to scan.}

\item{types}{Character vector of PII types to check. Defaults to all
available types from \code{\link[=pii_patterns]{pii_patterns()}}. Valid values: \code{"ssn"}, \code{"email"},
\code{"phone"}, \code{"credit_card"}, \code{"ip_address_v4"}, \code{"ip_address_v6"},
\code{"phone_intl"}, \code{"iban"}, \code{"dob"}, \code{"mac_address"}, \code{"us_passport"},
\code{"drivers_license"}, \code{"itin"}, \code{"vin"}.}
}
\value{
A named list where each element is a character vector of matches
found for that PII type. Empty character vectors indicate no matches.
}
\description{
Scans text for personally identifiable information using regex patterns.
}
\examples{
detect_pii("Call me at 555-123-4567 or email me at test@example.com")
detect_pii("SSN: 123-45-6789", types = "ssn")
}
