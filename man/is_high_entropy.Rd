% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output-entropy.R
\name{is_high_entropy}
\alias{is_high_entropy}
\title{Check if a string has high entropy}
\usage{
is_high_entropy(s, base64_threshold = 4.5, hex_threshold = 3, min_length = 20L)
}
\arguments{
\item{s}{Character(1). The string to check.}

\item{base64_threshold}{Numeric(1). Entropy threshold for base64-like
strings (default 4.5).}

\item{hex_threshold}{Numeric(1). Entropy threshold for hex-like strings
(default 3.0).}

\item{min_length}{Integer(1). Minimum string length to consider
(default 20). Shorter strings always return \code{FALSE}.}
}
\value{
Logical(1).
}
\description{
Determines whether a string has suspiciously high Shannon entropy,
suggesting it may be a secret, key, or random token.
}
\examples{
is_high_entropy("aaaaaaaaaaaaaaaaaaaaa")  # FALSE (low entropy)
is_high_entropy("aB3xK9pQ2mR7nL4wS8vD")  # likely TRUE
}
\seealso{
\code{\link[=shannon_entropy]{shannon_entropy()}}, \code{\link[=guard_output_entropy]{guard_output_entropy()}}
}
