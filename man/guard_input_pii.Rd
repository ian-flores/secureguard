% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-pii.R
\name{guard_input_pii}
\alias{guard_input_pii}
\title{Input PII guardrail}
\usage{
guard_input_pii(
  detect = c("ssn", "email", "phone", "credit_card"),
  action = c("block", "warn")
)
}
\arguments{
\item{detect}{Character vector of PII types to look for. Default:
\code{c("ssn", "email", "phone", "credit_card")}. See \code{\link[=pii_patterns]{pii_patterns()}} for
valid types.}

\item{action}{Character(1). What to do when PII is found: \code{"block"}
(default) fails the check, \code{"warn"} passes with advisory warnings.}
}
\value{
A guardrail object of class \code{"secureguard"} with type \code{"input"}.
}
\description{
Creates a guardrail that detects personally identifiable information in
input text and either blocks or warns.
}
\examples{
g <- guard_input_pii()
run_guardrail(g, "My SSN is 123-45-6789")
run_guardrail(g, "Please help me with R code")
}
