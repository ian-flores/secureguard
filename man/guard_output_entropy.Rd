% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output-entropy.R
\name{guard_output_entropy}
\alias{guard_output_entropy}
\title{Entropy output guardrail}
\usage{
guard_output_entropy(
  min_length = 20L,
  base64_threshold = 4.5,
  hex_threshold = 3,
  action = c("block", "redact", "warn")
)
}
\arguments{
\item{min_length}{Integer(1). Minimum token length to check (default 20).}

\item{base64_threshold}{Numeric(1). Entropy threshold for base64-like
strings (default 4.5).}

\item{hex_threshold}{Numeric(1). Entropy threshold for hex-like strings
(default 3.0).}

\item{action}{Character(1). What to do when high-entropy strings are found:
\itemize{
\item \code{"block"} (default): fail the check.
\item \code{"redact"}: pass but replace high-entropy tokens with \verb{[HIGH_ENTROPY]}.
\item \code{"warn"}: pass with advisory warnings.
}}
}
\value{
A guardrail object of class \code{"secureguard"} with type \code{"output"}.
}
\description{
Creates a guardrail that scans output for high-entropy substrings that
may indicate leaked secrets, tokens, or keys.
}
\examples{
g <- guard_output_entropy()
run_guardrail(g, "Nothing suspicious here")
run_guardrail(g, "token=aB3xK9pQ2mR7nL4wS8vDfG5hJ6kT0yU")
}
\seealso{
\code{\link[=shannon_entropy]{shannon_entropy()}}, \code{\link[=is_high_entropy]{is_high_entropy()}}
}
