% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{as_pre_execute_hook}
\alias{as_pre_execute_hook}
\title{Convert code guardrails to a securer pre-execute hook}
\usage{
as_pre_execute_hook(...)
}
\arguments{
\item{...}{Guardrail objects with \code{type = "code"}.}
}
\value{
A function with signature \verb{function(code)} that returns \code{TRUE} if
all guardrails pass, or \code{FALSE} (with warnings) if any fail.
}
\description{
Takes one or more code guardrails and returns a function suitable for
securer's \code{pre_execute_hook} parameter. The hook returns \code{FALSE} to block
code that fails any guardrail, or \code{TRUE} to allow it.
}
\examples{
hook <- as_pre_execute_hook(
  guard_code_analysis(),
  guard_code_complexity(max_ast_depth = 10)
)
hook("x <- 1 + 2")
hook("system('ls')")
}
