% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output-pii.R
\name{guard_output_pii}
\alias{guard_output_pii}
\title{PII output guardrail}
\usage{
guard_output_pii(detect = NULL, action = c("block", "redact", "warn"))
}
\arguments{
\item{detect}{Character vector of PII types to detect. Defaults to all types
from \code{\link[=pii_patterns]{pii_patterns()}}: \code{"ssn"}, \code{"email"}, \code{"phone"}, \code{"credit_card"},
\code{"ip_address"}.}

\item{action}{Character(1). What to do when PII is found:
\itemize{
\item \code{"block"} (default): fail the check.
\item \code{"redact"}: pass but replace PII with \verb{[REDACTED_SSN]} etc.
\item \code{"warn"}: pass with advisory warnings.
}}
}
\value{
A guardrail object of class \code{"secureguard"} with type \code{"output"}.
}
\description{
Creates a guardrail that scans output for personally identifiable
information (PII).
}
\examples{
g <- guard_output_pii()
run_guardrail(g, "My SSN is 123-45-6789")

g_redact <- guard_output_pii(action = "redact")
result <- run_guardrail(g_redact, "My SSN is 123-45-6789")
result@details$redacted_text
}
