% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patterns-secrets.R
\name{detect_secrets_decoded}
\alias{detect_secrets_decoded}
\title{Detect secrets in text with decode-then-scan}
\usage{
detect_secrets_decoded(text, types = NULL)
}
\arguments{
\item{text}{Character(1). The text to scan.}

\item{types}{Character vector of secret types to check. Defaults to all
available types from \code{\link[=secret_patterns]{secret_patterns()}}.}
}
\value{
A named list where each element is a character vector of matches
found for that secret type, de-duplicated across all decoded variants.
}
\description{
Scans the original text plus base64-decoded and URL-decoded variants for
secrets. This catches credentials that have been obfuscated via encoding.
}
\examples{
# Detect a base64-encoded AWS key
encoded <- base64enc::base64encode(charToRaw("AKIAIOSFODNN7EXAMPLE"))
detect_secrets_decoded(encoded)
}
